# Enforce org trust + pin SPKI for critical endpoints
# Place as ~/.curlrc or /etc/curlrc (user/system)
# Use SPKI pin from: openssl s_client -connect host:443 </dev/null 2>/dev/null | openssl x509 -pubkey -noout | openssl pkey -pubin -outform DER | openssl dgst -sha256 -binary | base64

# Use system store; optionally restrict with --cacert per command
# ca bundle honored by curl automatically; to override uncomment:
# cacert = /etc/ssl/certs/ca-certificates.crt

# Example pinned host:
# pinnedpubkey = "sha256//BASE64SPKIHASH_FOR_host"